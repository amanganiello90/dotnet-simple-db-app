version: '3.0'
 
services:
   db:
     image: mysql:5.7
     environment:
       MYSQL_RANDOM_ROOT_PASSWORD: 1
       MYSQL_DATABASE: myappdb
       MYSQL_USER: dbuser
       MYSQL_PASSWORD: dbuserpassword
     volumes:
       - dbdata:/var/lib/mysql
     ports:
       - "3306:3306"
 
   myapp:
     depends_on:
       - db
     links:
       - db
 #    image: amanganiello90/dotnet-app
     build:
       context: .
     ports:
       - "5000:80"
 
volumes:
    dbdata: